class Employee{
  String ? name; 
  String ? designation;
  double ? salary;
  // Employee({
  //   required this.name,
  //   required this.designation,
  //   required this.salary
  // }): assert(name != null, designation != null, salary > 0, "Please provide all required arguments");
}